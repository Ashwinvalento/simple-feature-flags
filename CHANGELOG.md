# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- `getFlag` method to `FeatureFlags` core class and `useFeatures` React hook to retrieve flag values with optional default.
- `isEnabled` and `isDisabled` now support an optional `defaultValue`.
- TSDoc comments for all public functions and interfaces.
- Automated release workflow using Changesets and GitHub Actions.
